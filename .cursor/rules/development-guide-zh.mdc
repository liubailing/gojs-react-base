---
description: 
globs: 
alwaysApply: false
---
# 开发指南

## 开发环境设置

### 1. 环境准备
```bash
# 使用 Devbox 设置开发环境（推荐）
devbox shell

# 或者确保安装了以下工具
# - Node.js 18+
# - pnpm 8+
```

### 2. 项目初始化
```bash
# 克隆项目
git clone <repository-url>
cd octopusx

# 安装依赖
pnpm install

# 验证安装
pnpm lint
```

## 开发工作流

### 1. 包开发
```bash
# 开发特定包
pnpm --filter <package-name> dev

# 构建特定包
pnpm --filter <package-name> build

# 测试特定包
pnpm --filter <package-name> test
```

### 2. Electron 应用开发
```bash
# 启动 Electron 应用开发模式
cd apps/octoparse
pnpm dev

# 构建应用
pnpm build

# 打包分发
pnpm build:linux
pnpm build:mac
pnpm build:win
```

### 3. 核心包开发

#### Runner 包
- **位置**: [packages/runner/](mdc:packages/runner)
- **功能**: 执行环境适配，支持本地和云端运行
- **开发**: `pnpm --filter runner dev`

#### Workflow 包
- **位置**: [packages/workflow/](mdc:packages/workflow)
- **功能**: 浏览器自动化和采集步骤执行
- **开发**: `pnpm --filter workflow dev`

#### Cloud-Runner 包
- **位置**: [packages/cloud-runner/](mdc:packages/cloud-runner)
- **功能**: 云端执行环境支持
- **开发**: `pnpm --filter cloud-runner dev`

## 调试指南

### 1. Electron 应用调试
```bash
# 启用开发者工具
pnpm dev

# 主进程调试
# 在 VS Code 中使用 Electron Main 调试配置

# 渲染进程调试
# 使用 Chrome DevTools (Ctrl+Shift+I)
```

### 2. 包调试
```bash
# 使用 Node.js 调试器
node --inspect-brk packages/runner/dist/index.js

# 或使用 VS Code 调试配置
```

## 测试策略

### 1. 单元测试
```bash
# 运行所有测试
pnpm test

# 运行特定包测试
pnpm --filter <package-name> test

# 覆盖率测试
pnpm test:coverage
```

### 2. 集成测试
```bash
# 端到端测试
pnpm test:e2e

# UI 测试 (使用 Vitest)
pnpm --filter octoparse test
```

## 代码质量保证

### 1. 代码检查
```bash
# ESLint 检查
pnpm lint

# 修复可自动修复的问题
pnpm lint:fix

# 类型检查
pnpm type-check
```

### 2. 代码格式化
```bash
# Prettier 格式化
pnpm format

# 检查格式
pnpm format:check
```

## 版本管理

### 1. 包版本管理
- 遵循语义化版本控制 (SemVer)
- 使用 [package.json](mdc:package.json) 中的版本号
- 依赖包版本需要在工作空间中保持一致

### 2. 发布流程
```bash
# 更新版本
pnpm version patch|minor|major

# 构建所有包
pnpm build

# 发布 (如果配置了私有 registry)
pnpm publish
```

## 性能优化

### 1. 构建优化
- 使用 Vite 进行快速构建
- 启用 TypeScript 增量编译
- 合理配置 webpack/vite 缓存

### 2. 运行时优化
- 避免在主线程执行重计算
- 合理使用 Web Workers
- 优化内存使用，避免内存泄漏

## 故障排除

### 1. 常见问题
```bash
# 依赖安装失败
rm -rf node_modules packages/*/node_modules
pnpm install

# 构建失败
pnpm run clean:dist
pnpm build

# 类型错误
pnpm type-check
```

### 2. 调试技巧
- 使用 `console.log` 进行简单调试
- 配置 VS Code 断点调试
- 使用 Chrome DevTools 性能分析
- 查看 Electron 主进程和渲染进程日志

## 最佳实践

1. **代码组织**: 按功能模块组织代码，保持单一职责原则
2. **类型安全**: 充分利用 TypeScript 类型系统
3. **错误处理**: 实现完善的错误处理和日志记录
4. **文档**: 保持代码注释和文档更新
5. **测试**: 编写有意义的测试用例
6. **性能**: 关注应用性能和用户体验
