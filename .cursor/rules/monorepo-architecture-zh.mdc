---
description: 
globs: 
alwaysApply: false
---
# Monorepo 架构指南

## 架构概览

八爪鱼数据采集器采用 monorepo 架构，使用 [pnpm](mdc:pnpm-workspace.yaml) 工作空间管理多个相关包。这种架构提供了代码复用、统一构建和版本管理的优势。

## 工作空间结构

### 1. 根目录配置
```
octopusx/
├── apps/                    # 应用程序
├── packages/                # 共享包
├── package.json            # 根包配置
├── pnpm-workspace.yaml     # 工作空间配置
├── tsconfig.json           # 共享 TS 配置
├── .eslintrc.js           # 共享 ESLint 配置
└── README.md              # 项目文档
```

### 2. 应用层 (apps/)
```
apps/
└── octoparse/              # 主要 Electron 应用
    ├── src/
    │   ├── main/           # 主进程
    │   ├── renderer/       # 渲染进程
    │   └── preload/        # 预加载脚本
    ├── package.json        # 应用依赖
    └── electron.vite.config.ts
```

### 3. 包层 (packages/)
```
packages/
├── runner/                 # 执行环境适配器
│   ├── src/
│   │   ├── shared/        # 共享模块
│   │   └── socket/        # Socket 通信
│   └── package.json
├── workflow/               # 工作流引擎
│   ├── src/
│   │   ├── actions/       # 动作定义
│   │   ├── configs/       # 配置管理
│   │   ├── enums/         # 枚举定义
│   │   ├── models/        # 数据模型
│   │   ├── translator/    # 转换器
│   │   └── utils/         # 工具函数
│   └── package.json
└── cloud-runner/           # 云端运行器
    ├── src/
    ├── scripts/
    └── package.json
```

## 包依赖关系

### 1. 依赖层次结构
```mermaid
graph TD
    A[octoparse App] --> B[runner]
    A --> C[workflow]
    A --> D[cloud-runner]
    B --> C
    D --> C
    C --> E[External Dependencies]
```

### 2. 包职责划分

#### Workflow 包 (核心引擎)
- **位置**: [packages/workflow/](mdc:packages/workflow)
- **职责**: 
  - 定义工作流执行逻辑
  - 浏览器自动化操作
  - 数据采集步骤管理
- **依赖**: 外部库（Puppeteer、Playwright 等）
- **被依赖**: runner, cloud-runner, octoparse

#### Runner 包 (执行适配器)
- **位置**: [packages/runner/](mdc:packages/runner)
- **职责**:
  - 本地执行环境适配
  - Socket 通信管理
  - 与客户端集成
- **依赖**: workflow
- **被依赖**: octoparse

#### Cloud-Runner 包 (云端执行器)
- **位置**: [packages/cloud-runner/](mdc:packages/cloud-runner)
- **职责**:
  - 云端执行环境支持
  - 分布式任务管理
  - 云服务集成
- **依赖**: workflow
- **被依赖**: octoparse

#### Octoparse 应用 (用户界面)
- **位置**: [apps/octoparse/](mdc:apps/octoparse)
- **职责**:
  - 用户界面展示
  - 用户交互处理
  - 桌面应用功能
- **依赖**: runner, workflow, cloud-runner

## 包管理策略

### 1. 版本管理
```json
// 根 package.json 版本策略
{
  "name": "octopus",
  "version": "9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
```

### 2. 依赖管理原则
- **共享依赖**: 在根 `package.json` 中管理公共依赖
- **特定依赖**: 在各包的 `package.json` 中管理特定依赖
- **版本一致性**: 确保相同依赖在不同包中版本一致
- **Peer Dependencies**: 用于可选依赖和版本兼容性

### 3. 内部包引用
```json
// packages/runner/package.json
{
  "dependencies": {
    "@octopus/workflow": "workspace:*"
  }
}
```

## 构建和发布策略

### 1. 构建顺序
```bash
# 按依赖顺序构建
pnpm --filter workflow build
pnpm --filter runner build
pnpm --filter cloud-runner build
pnpm --filter octoparse build
```

### 2. 开发模式
```bash
# 并行开发模式
pnpm --filter workflow dev &
pnpm --filter runner dev &
pnpm --filter cloud-runner dev &
pnpm --filter octoparse dev
```

### 3. 测试策略
```bash
# 运行所有包的测试
pnpm -r test

# 特定包测试
pnpm --filter workflow test
```

## 代码共享策略

### 1. 类型共享
```typescript
// packages/workflow/src/types/index.ts
export interface IWorkflowConfig {
  id: string;
  name: string;
  steps: IWorkflowStep[];
}

// packages/runner/src/index.ts
import type { IWorkflowConfig } from '@octopus/workflow';
```

### 2. 工具函数共享
```typescript
// packages/workflow/src/utils/common.ts
export function createId(): string {
  return `wf_${Date.now()}_${Math.random()}`;
}

// packages/runner/src/runner.ts
import { createId } from '@octopus/workflow/utils';
```

### 3. 常量共享
```typescript
// packages/workflow/src/constants/index.ts
export const DEFAULT_TIMEOUT = 30000;
export const MAX_RETRIES = 3;
```

## 配置管理

### 1. TypeScript 配置继承
```json
// packages/workflow/tsconfig.json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

### 2. ESLint 配置共享
```javascript
// packages/workflow/.eslintrc.js
module.exports = {
  extends: ['../../.eslintrc.js'],
  // 包特定规则
};
```

### 3. 构建配置
```typescript
// packages/workflow/vite.config.ts
import { defineConfig } from 'vite';
import { resolve } from 'path';

export default defineConfig({
  build: {
    lib: {
      entry: resolve(__dirname, 'src/index.ts'),
      formats: ['es', 'cjs']
    }
  }
});
```

## 最佳实践

### 1. 包设计原则
- **单一职责**: 每个包只负责一个明确的功能域
- **松耦合**: 包之间通过明确的接口通信
- **高内聚**: 包内功能紧密相关
- **可复用**: 设计通用的、可复用的组件

### 2. 依赖管理
- **最小依赖**: 只添加必要的依赖
- **版本固定**: 重要依赖使用固定版本
- **定期更新**: 定期更新依赖并测试兼容性
- **安全检查**: 定期检查依赖安全漏洞

### 3. 开发工作流
- **功能分支**: 每个功能使用独立分支开发
- **包级测试**: 确保每个包都有完整测试覆盖
- **集成测试**: 定期运行跨包集成测试
- **文档同步**: 保持文档与代码同步更新

### 4. 发布策略
- **语义版本**: 遵循语义化版本规范
- **变更日志**: 维护详细的变更记录
- **向后兼容**: 避免破坏性变更
- **渐进升级**: 提供平滑的升级路径

## 常见问题和解决方案

### 1. 循环依赖
```bash
# 检测循环依赖
pnpm list --depth=0

# 解决方案: 提取共享代码到独立包
```

### 2. 版本冲突
```bash
# 检查版本冲突
pnpm why <package-name>

# 解决方案: 统一版本或使用 overrides
```

### 3. 构建顺序问题
```bash
# 使用拓扑排序构建
pnpm -r --sort build
```

### 4. 开发环境同步
```bash
# 重置所有包
pnpm run clean:modules
pnpm install

# 重新构建
pnpm -r build
```
